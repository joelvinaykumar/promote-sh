# Work Log App Design

This is a code bundle for Work Log App Design. The original project is available at https://www.figma.com/design/8YVum9OTHeU1kQKJPusRps/Work-Log-App-Design.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
